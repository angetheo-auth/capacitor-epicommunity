import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'capacitor.epicommunity',
  appName: 'capacitor-epicommunity',
  webDir: 'dist'
};

export default config;
